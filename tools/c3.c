#include "kw0x.h"

int main(void) {
	int i;
	while(1) {
		/*
		PORTA.GPCLR = 0xFFFFFFFF;
		for(i = 0; i < 1000000; ++i);
		PORTA.GPCLR = 0xFFFFFFFF;
		for(i = 0; i < 1000000; ++i);
		*/
	}
	return 0;
}
